// Combine an Array into a String Using the join Method

/* Joining the elements of an array to create a string is
done with the join method. */

const arr = ["Hello", "World"];
const str = arr.join(" ");
console.log(str);  // Hello World